#ifndef MANAGEPROCESSOR_H
#define MANAGEPROCESSOR_H

#include <string>

using std::string;

bool ExecuteProcessor(const string& strPath);

bool TerminateProcessor(const string& strPath);

#endif // MANAGEPROCESSOR_H